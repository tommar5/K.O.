jQuery.extend(jQuery.validator.messages, {
    required: "{{ 'validators.required'|trans }}",
    email: "{{ 'validators.enter_valid_email'|trans }}",
    date: "{{ 'validators.enter_valid_date'|trans }}"
});

